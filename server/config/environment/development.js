'use strict';

// Development specific configuration
// ==================================
module.exports = {
  endpoints:[
    {
      route: '/api/comments',
      controller: './controllers/comments.controller'
    }
  ]
};
